Background Manager Setup :)
Change your backgrounds and generate them based on your interests, 
holidays and the current weather!

Enusre you have the following installed:
python3
pip install Pillow
pip install requests
pip install lxml

To run the app:
Go in the backgroundManager folder
Double click the .exe file 
The images are stored in the BG_Manager folder